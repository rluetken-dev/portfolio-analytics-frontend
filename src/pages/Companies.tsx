// src/pages/Companies.tsx
import React from "react";

/**
 * Companies page (placeholder).
 * Later this will list companies fetched from the backend
 * with sorting, filtering, and basic metrics.
 */
export default function Companies() {
  return (
    <div>
      <h2>🏢 Companies</h2>
      <p>This page will later show a list of companies with key fundamentals and metrics.</p>
    </div>
  );
}
